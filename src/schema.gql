# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Ticket {
  id: ID!
  title: String!
  description: String!
  priority: PriorityEnum!
  category: CategoryEnum!
  status: StatusEnum!
}

enum PriorityEnum {
  high
  medium
  low
}

enum CategoryEnum {
  incident
  support
  error
}

enum StatusEnum {
  pending
  verified
  approved
  rejected
}

type Query {
  """Hola mundo"""
  hello_world: String!

  """Hola to (name)"""
  hello_to(to: String): String!
  findAll: [Ticket!]!
}

type Mutation {
  createPost(ticketInput: CreateTicketInput!): Ticket!
}

input CreateTicketInput {
  title: String!
  description: String
  priority: PriorityEnum!
  category: CategoryEnum!
}